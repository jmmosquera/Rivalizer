<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iezv.jmm.rivalizer">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Views.ProfileView"
            android:label="@string/title_activity_profile_view"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Views.ChatView"
            android:label="@string/title_activity_chat_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.RivalView"
            android:label="@string/title_activity_rival_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.GenericView"
            android:label="@string/title_activity_generic_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Views.PlayerLocation"
            android:label="@string/title_activity_player_location" />
        <activity
            android:name=".Views.LoginView"
            android:label="@string/title_activity_login_view"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Views.PlaceView"
            android:label="@string/title_activity_place_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.PlaceRegister"
            android:label="@string/title_activity_place_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.PlayerRegister"
            android:label="@string/title_activity_player_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.GameRegister"
            android:label="@string/title_activity_game_register"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Views.GameView"
            android:label="@string/title_activity_game_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>